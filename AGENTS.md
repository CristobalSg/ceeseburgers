# Agent Instructions

- Always use `pnpm` for package management and project scripts in this repository.
- Do not use `npm` or `yarn` commands unless the user explicitly asks for them.
- Examples: use `pnpm install`, `pnpm run dev`, `pnpm run build`, and `pnpm run lint`.
